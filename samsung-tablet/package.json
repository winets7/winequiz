{
  "name": "vintaste-samsung-tablet",
  "version": "2.1.0",
  "private": true,
  "description": "Android-оболочка Wine Quiz для планшета Samsung (GeckoView → vintaste.ru)",
  "scripts": {
    "icons": "node scripts/copy-icons.mjs",
    "android:debug": "node scripts/ensure-local-properties.mjs && cd android && .\\gradlew.bat assembleDebug",
    "android:release": "node scripts/ensure-keystore.mjs && node scripts/ensure-local-properties.mjs && npm run icons && cd android && .\\gradlew.bat assembleRelease",
    "install:release": "node scripts/install-release.mjs"
  },
  "devDependencies": {
    "sharp": "^0.34.2"
  }
}
