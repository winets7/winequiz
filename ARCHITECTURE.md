# 🍷 Винная Викторина — Архитектура проекта

## Обзор

Интерактивная мультиплеерная викторина на винную тему. Основной интерфейс — браузер, с поддержкой планшетов и смартфонов (Android, iOS) через PWA.

---

## Стек технологий

| Технология | Назначение |
|------------|-----------|
| **Next.js (React)** + TypeScript | Фронтенд + API маршруты |
| **Tailwind CSS** | Адаптивная верстка, тёмная/светлая тема |
| **PostgreSQL** | База данных |
| **Prisma ORM** | Работа с БД, миграции, типизация |
| **Socket.io** | Мультиплеер в реальном времени |
| **NextAuth.js** | Аутентификация |
| **qrcode.react** | Генерация QR-кода для подключения игроков |
| **PWA** | Установка на мобильные устройства без магазинов |

---

## База данных: стратегия развёртывания

### Принцип: локальная разработка → облачный продакшн

Вся работа с БД идёт через **Prisma ORM**. Подключение определяется единственной переменной окружения `DATABASE_URL` в файле `.env`. Для переноса на любой облачный сервис достаточно **заменить строку подключения** — код приложения не меняется.

### Окружения

| Окружение | Подключение | Описание |
|-----------|-------------|----------|
| **Локальная разработка** | `postgresql://user:password@localhost:5432/winequiz` | PostgreSQL установлен на машине разработчика |
| **Продакшн (облако)** | `postgresql://user:password@host:5432/winequiz?sslmode=require` | Любой облачный провайдер |

### Рекомендуемые облачные сервисы для переноса

| Сервис | Бесплатный план | Особенности |
|--------|----------------|-------------|
| **Supabase** | 500 МБ, 2 проекта | Встроенный Auth, Realtime, REST API |
| **Neon** | 512 МБ, branching | Serverless, автомасштабирование, мгновенный старт |
| **Railway** | $5 кредит/мес | Простой деплой, авто-бэкапы |
| **Vercel Postgres** | 256 МБ | Нативная интеграция с Next.js / Vercel |
| **AWS RDS** | 12 мес. free tier | Максимальная гибкость, масштабируемость |

### Файл `.env`

```env
# === Локальная разработка ===
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/winequiz"

# === Продакшн (пример для Supabase) ===
# DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432/postgres?sslmode=require"

# === Продакшн (пример для Neon) ===
# DATABASE_URL="postgresql://[USER]:[PASSWORD]@[HOST].neon.tech/winequiz?sslmode=require"
```

### Миграции (Prisma)

Prisma обеспечивает **полную переносимость схемы** между окружениями:

```bash
# Разработка — создание и применение миграций
npx prisma migrate dev --name init

# Продакшн — применение миграций на облачной БД
npx prisma migrate deploy

# Заполнение начальными данными (вопросы, категории, достижения)
npx prisma db seed
```

### Важные правила

1. **`.env` НЕ коммитится в git** — добавлен в `.gitignore`
2. **`.env.example`** — шаблон с описанием переменных (без секретов), коммитится
3. **Миграции коммитятся** — папка `prisma/migrations/` всегда в git
4. **Seed-данные коммитятся** — `prisma/seed.ts` с вопросами и достижениями в git

---

## Ключевые параметры

- **Максимум игроков в сессии:** 99
- **Подключение игроков:** по QR-коду (содержит URL вида `https://domain/join/WN-XXXXXX`)
- **Тема интерфейса:** светлая / тёмная (выбор игрока, сохраняется в localStorage + cookie)
- **Вопросы:** статичные, хранятся в БД
- **Мультиплеер:** реальное время через Socket.io
- **Админ-панель:** CRUD вопросов, категорий, пользователей, история игр
- **Геймификация:** XP, уровни, достижения, рейтинг

---

## Структура проекта

```
distwinegame/
├── app/                          # Next.js App Router
│   ├── (auth)/                   # Страницы входа/регистрации
│   │   ├── login/
│   │   └── register/
│   ├── (game)/                   # Игровой интерфейс
│   │   ├── lobby/                # Лобби — ожидание игроков
│   │   ├── play/[gameId]/        # Игровая сессия
│   │   └── results/[gameId]/     # Результаты игры
│   ├── (dashboard)/              # Личный кабинет игрока
│   │   ├── profile/              # Профиль, уровень, XP
│   │   ├── achievements/         # Достижения
│   │   └── leaderboard/          # Рейтинг игроков
│   ├── admin/                    # Админ-панель
│   │   ├── questions/            # CRUD вопросов
│   │   ├── categories/           # Категории вопросов
│   │   ├── games/                # История игр
│   │   └── users/                # Управление пользователями
│   ├── api/                      # REST API маршруты
│   └── layout.tsx                # Корневой layout
├── components/                   # React-компоненты
│   ├── ui/                       # Базовые UI (кнопки, карточки...)
│   ├── game/                     # Игровые компоненты
│   └── admin/                    # Компоненты админки
├── lib/                          # Утилиты
│   ├── prisma.ts                 # Prisma client
│   ├── socket.ts                 # Socket.io клиент
│   └── auth.ts                   # NextAuth конфиг
├── server/                       # Серверная логика
│   └── socket/                   # Socket.io сервер + игровая логика
├── prisma/
│   ├── schema.prisma             # Схема БД
│   └── seed.ts                   # Начальные данные (вопросы)
├── public/                       # Статика + PWA
│   ├── manifest.json
│   └── icons/
└── package.json
```

---

## Схема базы данных

### User (Пользователь)

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| name | String | Имя / никнейм |
| email | String | Email (уникальный) |
| passwordHash | String | Хэш пароля |
| avatar | String? | URL аватара |
| role | Enum (ADMIN, PLAYER) | Роль пользователя |
| level | Int | Текущий уровень (1-50) |
| xp | Int | Накопленный опыт |
| createdAt | DateTime | Дата регистрации |

### Category (Категория)

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| name | String | Название (Красное, Белое, Регионы, Сорта, История) |
| icon | String? | Иконка категории |

### Question (Вопрос)

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| text | String | Текст вопроса |
| imageUrl | String? | Изображение к вопросу |
| difficulty | Enum (EASY, MEDIUM, HARD) | Сложность |
| categoryId | UUID → Category | Категория |
| timeLimit | Int | Лимит времени в секундах |
| createdAt | DateTime | Дата создания |

### Answer (Вариант ответа)

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| questionId | UUID → Question | Вопрос |
| text | String | Текст ответа |
| isCorrect | Boolean | Правильный ли ответ |

### GameSession (Игровая сессия)

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| hostId | UUID → User | Создатель игры |
| status | Enum (WAITING, PLAYING, FINISHED) | Статус |
| code | String | Код комнаты (6 символов, напр. WN-482917) |
| maxPlayers | Int | Макс. игроков (до 99) |
| currentRound | Int | Текущий раунд |
| totalRounds | Int | Всего раундов |
| createdAt | DateTime | Дата создания |

### GamePlayer (Участник игры)

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| gameId | UUID → GameSession | Игровая сессия |
| userId | UUID → User | Игрок |
| score | Int | Итоговый счёт |
| position | Int? | Место в рейтинге |
| joinedAt | DateTime | Время подключения |

### PlayerAnswer (Ответ игрока)

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| gamePlayerId | UUID → GamePlayer | Участник |
| questionId | UUID → Question | Вопрос |
| answerId | UUID? → Answer | Выбранный ответ (null если не ответил) |
| timeTaken | Int | Время ответа в миллисекундах |
| points | Int | Начисленные очки |
| isCorrect | Boolean | Правильный ли ответ |

### Achievement (Достижение)

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| name | String | Название |
| description | String | Описание |
| icon | String | Иконка |
| condition | String | Условие (JSON или код) |
| xpReward | Int | Награда в XP |

### UserAchievement (Полученное достижение)

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | Первичный ключ |
| userId | UUID → User | Пользователь |
| achievementId | UUID → Achievement | Достижение |
| unlockedAt | DateTime | Дата получения |

---

## Игровой поток (Real-time)

```
Хост (ПК / планшет)            Сервер                  Игрок (смартфон)
───────────────────            ────────                 ─────────────────
        │                         │                          │
  Создаёт игру                    │                          │
        ├── createGame ──────────►│                          │
        │◄── gameCode: WN-482917 ─┤                          │
        │                         │                          │
  QR-код на экране                │              Сканирует QR камерой
                                  │◄──── joinGame(code) ─────┤
        │◄── playerJoined ────────┤───── playerJoined ──────►│
        │                         │                          │
  Нажимает "Начать"               │                          │
        ├── startGame ───────────►│                          │
        │                         ├───── question ──────────►│
        │                         │          (таймер N сек)   │
        │                         │◄──── answer ─────────────┤
        │                         │  (сервер собирает все     │
        │                         │   ответы, считает очки)   │
        │◄── roundResults ────────┤───── roundResults ──────►│
        │                         │                          │
        │         ... повторяется для каждого раунда ...      │
        │                         │                          │
        │◄── gameOver + rankings ─┤───── gameOver + rankings►│
        │                         ├───── checkAchievements ─►│
        │                         ├───── updateXP/Level ────►│
```

---

## Система геймификации

### XP и уровни

- **XP за правильный ответ:** базовые очки × множитель скорости
- **Множитель скорости:** чем быстрее ответ, тем больше XP (от ×1.0 до ×2.0)
- **Формула уровня:** Level N требует N² × 100 XP
- **Максимальный уровень:** 50

### Звания (привязаны к уровням)

| Уровни | Звание |
|--------|--------|
| 1–5 | Новичок |
| 6–15 | Любитель |
| 16–25 | Знаток |
| 26–35 | Сомелье |
| 36–45 | Энолог |
| 46–50 | Мастер вина |

### Достижения (примеры)

| Достижение | Условие |
|------------|---------|
| 🥂 Первый глоток | Сыграть первую игру |
| 🎯 Меткий стрелок | 10 правильных ответов подряд |
| ⚡ Спринтер | Ответить правильно менее чем за 2 секунды |
| 🏆 Непобедимый | 5 побед подряд |
| 📚 Энциклопедист | Правильно ответить на вопросы из всех категорий |
| 🍷 Сомелье | 100 правильных ответов |
| 👑 Легенда | Достичь 50-го уровня |

### Рейтинг

- Глобальная таблица лидеров по общему XP
- Рейтинг по винрейту (процент побед)
- Топ-10 отображается всегда + позиция текущего игрока

---

## Тема интерфейса (светлая / тёмная)

Реализация: **Tailwind CSS `darkMode: 'class'`**

Выбор темы сохраняется в `localStorage` + `cookie` (для SSR без мерцания).

### Цветовая палитра (винная тематика)

| Элемент | Светлая тема | Тёмная тема |
|---------|-------------|-------------|
| Фон | `#FFFBF5` (тёплый кремовый) | `#1A1118` (тёмный бордо) |
| Карточки | `#FFFFFF` | `#2D1F2B` |
| Акцент основной | `#722F37` (бургундский) | `#C2485B` (светлый бордо) |
| Акцент вторичный | `#DAA520` (золотой) | `#F0C75E` (светлое золото) |
| Текст основной | `#1F1215` | `#F5EDE8` |
| Текст вторичный | `#6B5C5E` | `#A89496` |
| Успех (правильно) | `#2D6A4F` | `#52B788` |
| Ошибка (неправильно) | `#9B2226` | `#E5383B` |

---

## Подключение игроков (QR-код)

1. Хост создаёт игру → сервер генерирует уникальный код (напр. `WN-482917`)
2. На экране хоста отображается QR-код с URL: `https://domain/join/WN-482917`
3. Игрок сканирует QR нативной камерой телефона
4. Открывается браузер → страница подключения
5. Игрок вводит имя (или уже авторизован) → подключается через Socket.io
6. На экране хоста появляется новый игрок в списке лобби

**Библиотека:** `qrcode.react`

---

## Технические решения для 99 игроков

| Аспект | Решение |
|--------|---------|
| Socket.io нагрузка | Комнаты (rooms) — бродкаст в комнату, а не каждому отдельно |
| Сбор ответов | Сервер собирает все ответы за раунд пакетно, отправляет один `roundResults` |
| Таймаут ответа | Не ответил за N секунд — автоматически 0 очков |
| Переподключение | Socket.io авто-переподключение, сервер восстанавливает состояние |
| Отображение лобби | Виртуализированный список при большом количестве, счётчик подключённых |
| Отображение рейтинга | Топ-10 + позиция текущего игрока, полный список по запросу |

---

## Макет экрана лобби (хост)

```
┌─────────────────────────────────────────┐
│          🍷 ВИННАЯ ВИКТОРИНА            │
│                                          │
│      ┌─────────────┐                    │
│      │ ██████████  │   Код комнаты:     │
│      │ ██ QR ████  │   WN-482917        │
│      │ ██████████  │                    │
│      │ ██████████  │   Отсканируйте     │
│      └─────────────┘   чтобы войти      │
│                                          │
│  Игроки (7/99):                         │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐   │
│  │ Анна │ │ Макс │ │ Ира  │ │ Пётр │   │
│  └──────┘ └──────┘ └──────┘ └──────┘   │
│  ┌──────┐ ┌──────┐ ┌──────┐            │
│  │ Лена │ │ Олег │ │ Юля  │            │
│  └──────┘ └──────┘ └──────┘            │
│                                          │
│        [ 🚀 НАЧАТЬ ИГРУ ]               │
│                                          │
└─────────────────────────────────────────┘
```
