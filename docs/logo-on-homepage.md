# Размещение логотипа на первой странице

## Шаг 1. Подготовить файл логотипа

- **Формат:** лучше всего SVG (масштабируется без потери качества) или PNG с прозрачным фоном.
- **Размер:** для PNG ориентируйтесь на 200–400 px по ширине (или высоте для вертикального лого).
- **Имя файла:** например `logo.svg` или `logo.png`.

## Шаг 2. Положить файл в папку `public`

Скопируйте файл логотипа в каталог:

```
e:\distwinegame\public\
```

Итоговый путь может быть, например:
- `public/logo.svg`
- или `public/logo.png`

Файлы из `public` доступны по корню сайта: `https://ваш-сайт.ru/logo.svg`.

## Шаг 3. Подключение на главной странице (уже сделано)

На главной странице (`src/app/page.tsx`) вместо эмодзи 🍷 используется компонент `Image`: показывается картинка по пути `/logo.svg`. Если файла нет, отображается эмодзи (запасной вариант).

Чтобы использовать другой файл (например `logo.png`), замените в коде:
- путь с `/logo.svg` на `/logo.png`,
- при необходимости расширение в `alt` или комментарии.

## Шаг 4. Проверка

1. Убедитесь, что файл лежит в `public/logo.svg` (или выбранном вами имени).
2. Перезапустите dev-сервер (`npm run dev`), если он был запущен.
3. Откройте главную страницу в браузере — сверху должен отображаться логотип.

## Рекомендации

- **Тёмная/светлая тема:** если нужны два варианта логотипа (светлый и тёмный), можно положить `logo-light.svg` и `logo-dark.svg` и переключать их в коде в зависимости от темы.
- **Размер на экране:** при необходимости измените в `page.tsx` значения `width` и `height` у компонента `Image` (и соответствующие классы, например `w-32 h-32`).
